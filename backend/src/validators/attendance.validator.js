// التحقّق من بيانات الحضور.
//
// مفيش أي دالة تحقّق هنا، وده مقصود:
// مسارات الحضور كلها مابتاخدش body خالص —
//   POST /checkout بياخد المدرس من التوكن (req.user) مش من الطلب،
//   و GET /me و GET / مجرد قراءة.
// سيبنا الملف موجود عشان كل feature يبقى ليها نفس الشكل (validator + service + controller + routes)،
// ولو احتجنا تحقّق بعدين نضيفه هنا على طول.

module.exports = {};
